// 网站检查相关常量
export const SITE_STATUS = {
  STELLAR: 'stellar',
  NOT_STELLAR: 'not_stellar',
  ERROR: 'error'
};

// Issue 标签相关常量
export const ISSUE_LABELS = {
  NOT_STELLAR: '非Stellar站点',
  NETWORK_ERROR: '无法访问'
};

// 默认配置
export const DEFAULT_CONFIG = {
  timeout: 10000,
  retry_times: 3
};

// API 相关常量
export const API = {
  META_TAG: 'meta[name="hexo-theme"]',
  THEME_NAME: 'Stellar'
};

// 文件路径相关常量
export const PATHS = {
  CONFIG: 'config.yml',
  DATA: 'v2/data.json'
};